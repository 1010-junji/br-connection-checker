{
  "name": "br-setup-toolkit",
  "version": "0.3.0",
  "main": "dist/main.js",
  "scripts": {
    "build:main": "esbuild src/main.ts --bundle --platform=node --outfile=dist/main.js --external:electron",
    "build:preload": "esbuild src/preload.ts --bundle --platform=node --outfile=dist/preload.js --external:electron",
    "build": "npm-run-all --parallel build:*",
    "start": "npm run build && electron .",
    "watch:main": "npm run build:main -- --watch",
    "watch:preload": "npm run build:preload -- --watch",
    "watch": "npm-run-all --parallel watch:*",
    "package": "npm run build && electron-builder"
  },
  "keywords": [
    "BizRobo"
  ],
  "author": "JO",
  "license": "MIT",
  "description": "BizRobo! の運用環境のセットアップをスムーズに進めるための各種便利ツールの詰め合わせ",
  "engines": {
    "node": "^20.18.1"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.7",
    "@types/mysql": "^2.15.27",
    "@types/node": "^24.0.6",
    "@types/ping": "^0.4.4",
    "@types/xmldom": "^0.1.34",
    "electron": "^37.1.0",
    "electron-builder": "^26.0.12",
    "esbuild": "^0.25.5",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "adm-zip": "^0.5.16",
    "electron-serve": "^1.3.0",
    "mysql2": "^3.14.2",
    "xmldom": "^0.6.0"
  },
  "build": {
    "appId": "com.bizrobo.opekai.br-setup-toolkit",
    "productName": "BR Setup Toolkit",
    "win": {
      "icon": "assets/icon.ico",
      "target": [
        "nsis",
        "portable"
      ]
    },
    "files": [
      "dist/**/*",
      "assets/**/*"
    ],
    "directories": {
      "output": "release"
    },
    "extraResources": [
      {
        "from": "../client/dist/setup-toolkit-app",
        "to": "app",
        "filter": [
          "**/*"
        ]
      }
    ]
  }
}
